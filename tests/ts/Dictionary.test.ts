import { describe, expect, test } from "@jest/globals";
// import Dictionary from "@/DataStructures/ts/Dictionary";

// let d = new Dictionary<string, number>();

describe("testing Dictionary Data Structure", () => {
  test("empty string should result in zero", () => {
    //   d.add('2025-04-03', 1);
    // expect(d.find('2025-04-03')).toBe(5);
    expect(1).toBe(1);
  });
});
