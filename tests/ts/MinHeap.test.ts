import { describe, expect, test } from '@jest/globals';
// import { MinHeap } from '@/DataStructures/ts/Heap/MinHeap';

describe('testing Min. Heap data structure', () => {
    // const q = new MinHeap<number>();
    test('add 1 person to the queue so count is 1', () => {
        //     q.insert(1);
        //     expect(q.getMin()).toEqual(1)
        expect(true).toBeTruthy(); // Placeholder for actual test
    })
})